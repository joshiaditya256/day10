let myText =document.querySelector('h1')
function abc(){
    if(myText.textContent == ' iam in gfg bath') {
        myText.textContent =' i like  mern batch' 
    } else {
        myText.textContent = ' iam in gfg bath'
    }
}